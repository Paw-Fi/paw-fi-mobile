name: moneko
description: AI Personal Finance Coach & Budgeting App
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library//archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: "1.4.6+110"

environment:
  sdk: '>=3.1.5 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  cupertino_icons: ^1.0.2
  csv: ^6.0.0
  dio: ^5.4.0
  fl_chart: ^0.69.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_svg: ^2.0.9
  flutter_dotenv: ^5.1.0
  flutter_hooks: ^0.20.4
  freezed_annotation: ^2.4.1
  go_router: ^13.0.1
  hooks_riverpod: ^2.4.9
  http: ^1.1.0
  intl: ^0.20.2
  json_annotation: ^4.8.1
  retrofit: ^4.0.3
  riverpod_annotation: ^2.3.3
  shared_preferences: ^2.2.2
  supabase_flutter: ^2.10.3
  url_launcher: ^6.2.2
  vector_math: ^2.1.4
  image_picker: ^1.0.7
  image_cropper: ^11.0.0
  flutter_image_compress: ^2.3.0
  flutter_timezone: ^5.0.1
  camera: ^0.11.3
  archive: ^3.6.1
  path_provider: ^2.1.2
  permission_handler: ^12.0.1
  file_picker: ^8.0.0
  flutter_colorpicker: ^1.1.0
  ios_color_picker: ^2.0.2
  app_settings: ^5.2.0
  markdown_widget: ^2.3.2+6
  app_links: ^6.4.1
  package_info_plus: ^9.0.0
  firebase_core: ^4.2.1
  firebase_messaging: ^16.0.4
  firebase_crashlytics: ^5.0.4
  flutter_local_notifications: ^19.5.0
  app_badge_plus: ^1.2.6
  share_plus: ^10.0.0
  skeletonizer: ^2.0.0
  flutter_slidable: ^4.0.3
  web: ^1.1.0
  flutter_zoom_drawer: ^3.2.0
  adaptive_platform_ui: ^0.1.100
  crypto: ^3.0.3
  sign_in_with_apple: ^6.1.1
  reorderable_grid_view: ^2.2.8
  plaid_flutter: ^5.0.5
  home_widget: ^0.9.0
  collection: ^1.18.0
  record: ^6.1.2
  excel: ^4.0.6
  in_app_purchase: ^3.2.1
  in_app_purchase_android: ^0.4.0
  in_app_purchase_storekit: ^0.4.3
  in_app_review: ^2.0.11
  device_info_plus: ^12.3.0

dev_dependencies:
  build_runner: ^2.4.12
  custom_lint: ^0.5.8


  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.7.1
  retrofit_generator: ^8.0.6
  riverpod_generator: ^2.3.9
  riverpod_lint: ^2.3.7
  flutter_launcher_icons: ^0.13.1
  freezed: ^2.4.7
  mocktail: ^1.0.3
  fake_async: ^1.3.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  generate: true
  # To add assets to your application, add an assets section, like this:
  assets:
    - dotenv-prod
    - dotenv-dev
    - lib/assets/images/logo192.png
    - lib/assets/images/whatsapp/receipt.png
    - lib/assets/images/onboarding/
    - lib/assets/images/flags/
    - lib/assets/images/household/
    - lib/assets/images/whatsapp/text.png
    - lib/assets/images/audio-message.png
    - lib/assets/gifs/loading-anim.gif
    - lib/assets/gifs/splashscreen-loading.gif
    - lib/assets/mascots/
    - lib/assets/images/avatar/
    - lib/assets/images/avatar/1.face/
    - lib/assets/images/avatar/2.ears/
    - lib/assets/images/avatar/3.shirts/
    - lib/assets/images/avatar/4.hair/
    - lib/assets/images/avatar/5.brow/
    - lib/assets/images/avatar/6.eyes/
    - lib/assets/images/avatar/7.nose/
    - lib/assets/images/avatar/8.mouth/
    - lib/assets/images/avatar/9.blush/
    - lib/assets/images/avatar/10.accessories/
    - lib/assets/images/avatar/11.stars/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

flutter_launcher_icons:
  android: true
  ios: true
  image_path_android: "lib/assets/images/logo192.png"
  image_path_ios: "lib/assets/images/logo192.png"
  remove_alpha_ios: true
